kind: pipeline
type: docker
name: sleep

steps:
- name: greeting
  image: alpine
  commands:
  - echo hello
  - sleep 30
  - echo world

---
kind: pipeline
type: docker
name: default

steps:
- name: greeting
  image: alpine
  commands:
  - echo hello
  - echo world
